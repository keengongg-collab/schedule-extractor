# 隐私保护与数据脱敏声明

## 版权声明

本项目（轻量化AI文档排班日程提取工具）所有源代码及文档版权归项目贡献者所有，采用 [MIT 许可证](./LICENSE) 开源发布。

- **内容所有权**：源代码、文档、项目结构的设计与实现均属于项目贡献者
- **使用权限**：在保留版权声明的前提下，允许自由使用、修改、分发、再授权
- **禁止行为**：不得删除或篡改原始版权声明；不得将本软件用于非法用途

## 数据脱敏说明

本项目在设计阶段已实施以下脱敏措施，确保不泄露任何个人敏感信息：

### 1. 源代码层面

| 脱敏项 | 处理方式 | 说明 |
|--------|----------|------|
| 个人姓名 | 使用占位符 | 代码示例中使用"张三""李四"等通用占位符姓名，非真实人员 |
| 身份证号 | 不涉及 | 代码中不包含任何真实身份证号 |
| 手机号码 | 不涉及 | 代码中不包含任何真实手机号 |
| 电子邮箱 | 使用环境变量 | 所有 API 密钥、邮箱等配置均通过 `.env` 文件加载，不硬编码 |
| 服务器地址 | 使用 localhost | 示例配置仅使用 `127.0.0.1`，不包含真实服务器地址 |
| 微信凭据 | 使用环境变量 | 微信模板 ID 通过环境变量配置，不硬编码到代码中 |

### 2. 数据存储层面

- SQLite 数据库文件（`.db`）已在 `.gitignore` 中排除，不会被提交到仓库
- 上传的原始文档存放在 `backend/data/uploads/` 目录，已被 `.gitignore` 排除
- AI 问答记录中的用户输入仅存储在本地数据库，不会上传到远程服务器

### 3. 运行时层面

- AI 抽取模块（`extractor.py`）调用大模型 API 时，仅传递排班文本内容，不传递用户身份信息
- 微信小程序端不收集用户手机号、地理位置等隐私信息
- PC 端划词插件仅捕获用户主动选中的文本，不记录键盘输入历史

### 4. 配置文件

- `.env.example` 仅包含配置模板，不含任何真实凭据
- `.env` 文件（实际配置）已被 `.gitignore` 排除
- 所有敏感配置项均通过环境变量加载，详见 `backend/config.py`

## 合规说明

本项目遵守以下原则：
1. **最小必要原则**：仅采集排班提取所需的最少数据
2. **本地优先原则**：数据优先存储在用户本地，不强制上传云端
3. **用户知情原则**：AI 提问补充信息时明确告知用户用途
4. **可删除原则**：用户可随时删除所有本地数据（删除 SQLite 文件即可）

---

> 本声明随项目源代码一同发布，任何使用、修改或分发本项目的行为均视为已阅读并同意本声明内容。
